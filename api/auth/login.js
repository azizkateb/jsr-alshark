import {handle,nonce,seal,setCookie,redirect,digest,fail} from '../../server/studio-security.mjs';import {config,oauthCookie} from '../../server/studio-config.mjs';
export default handle(async(req,res)=>{if(req.method!=='GET')fail(405,'METHOD','GET فقط.');const state=nonce(),verifier=nonce();setCookie(res,oauthCookie,seal({state,verifier,exp:Date.now()+600000},'oauth'),600);const url=new URL('https://github.com/login/oauth/authorize');url.search=new URLSearchParams({client_id:config.clientId,redirect_uri:config.origin+'/api/auth/callback',state,code_challenge:digest(verifier),code_challenge_method:'S256',login:'azizkateb'}).toString();redirect(res,url.href)});
