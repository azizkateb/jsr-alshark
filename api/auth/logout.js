import {handle,session,csrfCheck,clearCookies,json,fail} from '../../server/studio-security.mjs';import {config} from '../../server/studio-config.mjs';
export default handle(async(req,res)=>{if(req.method!=='POST')fail(405,'METHOD','POST فقط.');const s=session(req);csrfCheck(req,s);let revoked=false;try{const r=await fetch('https://api.github.com/applications/'+config.clientId+'/token',{method:'DELETE',headers:{Authorization:'Basic '+Buffer.from(config.clientId+':'+process.env.GITHUB_CLIENT_SECRET).toString('base64'),Accept:'application/vnd.github+json','Content-Type':'application/json','X-GitHub-Api-Version':'2022-11-28'},body:JSON.stringify({access_token:s.token}),signal:AbortSignal.timeout(10000),redirect:'error'});revoked=r.ok||r.status===404}catch{}clearCookies(res);json(res,200,{ok:true,revoked})});
